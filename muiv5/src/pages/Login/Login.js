import React from 'react'
import Grid from '@mui/material/Grid';

const Login=() => {
    return (
        <Grid item xs={9}>
            <div>Login</div>
        </Grid>
    )
}

export default Login